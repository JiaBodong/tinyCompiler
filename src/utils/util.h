/*
    This file contains the implementation of the utility functions.
*/

#ifndef __MMUTIL_H__
#define __MMUTIL_H__

#include "Absyn.H"

/*
enum { 
    is_EVar, is_ELitInt, is_ELitDoub, is_ELitTrue, 
    is_ELitFalse, is_EApp, is_EString, is_Neg, is_Not, 
    is_EMul, is_EAdd, is_ERel, is_EAnd, is_EOr }
*/
// #define EXPR_ENUM_TO_STRING(enum) \
//     (enum == is_EVar) ? "EVar" : \
//     (enum == is_ELitInt) ? "ELitInt" : \
//     (enum == is_ELitDoub) ? "ELitDoub" : \
//     (enum == is_ELitTrue) ? "ELitTrue" : \
//     (enum == is_ELitFalse) ? "ELitFalse" : \
//     (enum == is_EApp) ? "EApp" : \
//     (enum == is_EString) ? "EString" : \
//     (enum == is_Neg) ? "Neg" : \
//     (enum == is_Not) ? "Not" : \
//     (enum == is_EMul) ? "EMul" : \
//     (enum == is_EAdd) ? "EAdd" : \
//     (enum == is_ERel) ? "ERel" : \
//     (enum == is_EAnd) ? "EAnd" : \
//     (enum == is_EOr) ? "EOr" : "Unknown"


#endif /* __MMUTIL_H__ */ 